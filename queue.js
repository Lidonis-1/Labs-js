'use strict'

const queue={
    "q": [{"item": "", "priority": 0}],
    "enqueue":(item, priorety)=>{this.q.},
    "dequeue":(cas)=>{
        switch(a){ //"highest, lowest, oldest, newest"
            case "highest":
                this.q.;
            case "lowest":
            case "oldest":
            case "newest":
        }
        
    }
}
queue.enqueue("lol")


